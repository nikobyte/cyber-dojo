#ifndef CALC_STATS_INCLUDED
#define CALC_STATS_INCLUDED

#include <vector>

using namespace std;

void calc_stats(const vector<int> &);
int minimum(const vector<int> &);
int maximum(const vector<int> &);
int num_elements(const vector<int> &);
float average(const vector<int> &);

#endif
